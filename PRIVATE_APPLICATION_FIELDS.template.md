# Private Application Fields Template

Do not commit a completed private-fields file.

If Vineesha chooses to keep private application details locally, copy this file to `PRIVATE_APPLICATION_FIELDS.md`. That file is ignored by git.

Never store passwords, Gmail secrets, API keys, tokens, SSN, DOB, passport, payment details, or private identity values in committed files.
